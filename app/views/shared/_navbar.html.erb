<nav class="bottom-tabs" aria-label="Primary">

  <!-- Brand (hidden on mobile, visible on desktop) -->
  <div class="nav-brand">
    <%= link_to root_path, class: "brand-link" do %>
      🥬 <span>Where's My Tofu</span>
    <% end %>
  </div>

  <!-- Navigation Links -->
  <div class="nav-links">

    <%= link_to root_path,
        class: "bottom-tab #{'active' if current_page?(root_path)}" do %>
      <i class="fa-solid fa-house"></i>
      <span>Home</span>
    <% end %>

    <%= link_to lists_index_path,
        class: "bottom-tab #{'active' if current_page?(lists_index_path)}" do %>
      <i class="fa-solid fa-list"></i>
      <span>Lists</span>
    <% end %>

    <% if user_signed_in? %>
      <%= link_to destroy_user_session_path,
          data: { turbo_method: :delete },
          class: "bottom-tab" do %>
        <i class="fa-solid fa-right-from-bracket"></i>
        <span>Logout</span>
      <% end %>
    <% else %>
      <%= link_to new_user_session_path,
          class: "bottom-tab #{'active' if current_page?(new_user_session_path)}" do %>
        <i class="fa-solid fa-user"></i>
        <span>Login</span>
      <% end %>
    <% end %>

  </div>
</nav>
