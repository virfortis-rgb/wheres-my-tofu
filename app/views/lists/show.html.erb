<% content_for :meta_title, "#{@list.name.capitalize} is on #{DEFAULT_META["meta_product_name"]}" %>
<div class="sticky-top bg-white border-bottom px-3 py-3 d-flex align-items-center" style="z-index: 1020;">
  <%= link_to lists_path, class: "back-link" do %>
    <i class="fa-solid fa-chevron-left"></i> Back
  <% end %>
  <h1 class="mb-0 ms-4 fs-5 fw-bold text-dark d-flex align-items-center">
    <%= image_tag "Tofu Logo.png", alt: "Tofu Logo", class: "me-2", style: "width: 24px; height: 24px; object-fit: contain;" %>
    <%= @list.name.capitalize %>
  </h1>

</div>

<div class="show-list-page px-3 pt-4 pb-2">
    <%# ── ADD ITEM FORM ── %>
    <div class="px-2 mb-3">
      <%= form_with url: attach_list_items_path(@list),
                  scope: :item,
                  method: :post,
                  id: "new_item_form",
                  class: "add-item-form" do %>
        <%= label_tag :query, "Add an Item", class: "visually-hidden" %>
        <%= text_field_tag :query,
          params[:query],
          class: "add-item-input",
          placeholder: "Add an item…" %>
        <%= submit_tag "+",
          name: "",
          class: "add-item-btn" %>
      <% end %>
    </div>
    <div class="favorite-stores-card mb-3">
      <%= form_with model: @list, url: favorite_stores_list_path(@list), method: :patch, class: "favorite-stores-form" do %>
        <p class="favorite-stores-label mb-2">What stores you want to go to today?</p>
        <%= hidden_field_tag "list[favorite_store_ids][]", "" %>
        <div class="favorite-stores-grid mb-3">
          <% @stores.each do |store| %>
            <label class="favorite-store-option">
              <%= check_box_tag "list[favorite_store_ids][]", store.id, @list.favorite_store_ids.include?(store.id) %>
              <%= store_logo_tag(store.name, size: 20) %>
              <span><%= store.name %></span>
            </label>
          <% end %>
        </div>
        <div class="favorite-stores-actions">
          <%= submit_tag "Apply Stores", class: "btn btn-primary btn-sm rounded-4" %>
          <%= button_tag "Reset",
                        type: "submit",
                        name: "clear_filter",
                        value: "1",
                        class: "btn btn-outline-secondary btn-sm rounded-4" %>
        </div>
      <% end %>
    </div>
    <%# ── ITEMS ── %>
    <div class="lists-body pb-5">
      <div class="list-grouped-card mb-4" id="grouped-items-container"> <%#  we can add some style here%>
        <%= render "items/grouped_items", items: @items, list: @list %>
        <% if @items.empty? %>
          <div id="empty-state" class="lists-empty text-center py-5">
            <%= image_tag "Tofu Logo.png", alt: "Tofu Logo", width: 100, class: "brand-logo" %>
            <p class="mt-3 text-muted">You haven't added anything yet.<br>
              Add your first one above!</p>
          </div>
        <% end %>
      </div>
      <%= render "lists/total", list: @list, items: @items %>
    </div>
  </div>
  <%# ── MAP ── %>
  <div class="px-2 mb-3">
    <%= render partial: "lists/map", locals: { list: @list } %>
  </div>
</div>
